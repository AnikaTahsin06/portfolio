const container = document.querySelectorAll('.container');
const allBtn = document.querySelectorAll('.controls');
const singleBtn = document.querySelectorAll('.control');
const allContainers = document.querySelector('.main-content');

console.log('array: ', [container, allBtn, singleBtn, allContainers])

